<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <title><%= title || 'LIS' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <style>
    .container { max-width: 980px; margin: 12px auto; font-family: Arial, Helvetica, sans-serif }
    label { display:inline-block; min-width:90px }
    input, select { margin:0 8px 8px 0 }
    table { border-collapse: collapse }
    table td, table th { padding:4px; border:1px solid #ccc }
    button { padding:6px 10px }
  </style>
</head>
<body>
  <div class="container">
    <h1><%= title || '' %></h1>
