<div class="card">
  <h3>My Profile</h3>

  <form action="/users/profile?_method=PUT" method="POST" enctype="multipart/form-data">
    <label>Name</label>
    <input type="text" name="name" value="<%= user.name %>" required />

    <label>Email</label>
    <input type="email" name="email" value="<%= user.email %>" required />

    <label>License Number</label>
    <input type="text" name="licenseNumber" value="<%= user.licenseNumber || '' %>" />

    <label>E-signature (PNG)</label>
    <% const sigPath = user.signature ? ('/assets/signature/' + user.signature) : null; %>
    <% if (sigPath) { %>
      <div style="margin-bottom:8px;"><img src="<%= sigPath %>" alt="signature" style="height:64px;border:1px solid #eee;padding:6px;background:#fff;" /></div>
    <% } %>
    <input type="file" name="signature" accept="image/png,image/*" />

    <label>Auto-apply signature</label>
    <% const as = user.autoSignature || { enabled:false, until:null }; const now = new Date(); const until = as && as.until ? new Date(as.until) : null; %>
    <select name="autoSignatureOption">
      <option value="off" <% if (!as.enabled) { %>selected<% } %>>Off</option>
      <option value="1day" <% if (as.enabled && until && (until - now) > 0 && (until - now) <= 24*3600*1000) { %>selected<% } %>>1 day</option>
      <option value="1week" <% if (as.enabled && until && (until - now) > 0 && (until - now) <= 7*24*3600*1000) { %>selected<% } %>>1 week</option>
      <option value="1month" <% if (as.enabled && until && (until - now) > 0 && (until - now) <= 31*24*3600*1000) { %>selected<% } %>>1 month</option>
      <option value="permanent" <% if (as.enabled && !as.until) { %>selected<% } %>>Always</option>
    </select>

    <hr />
    <h4>Change Password (optional)</h4>
    <label>New password</label>
    <input type="password" name="password" />
    <label>Confirm password</label>
    <input type="password" name="confirmPassword" />

    <div style="margin-top:12px;">
      <button class="button btn-primary" type="submit">Save Profile</button>
      <a href="/dashboard" class="button btn-secondary">Cancel</a>
    </div>
  </form>
</div>
