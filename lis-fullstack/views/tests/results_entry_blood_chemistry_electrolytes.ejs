<div class="card">
  <h3>Enter Blood Chemistry - Electrolytes Results - <%= test.testId %></h3>

  <form action="/tests/<%= test.id %>/results" method="POST">
    <input type="hidden" name="_method" value="POST">

    <div style="display:flex;gap:12px;align-items:center;margin-bottom:8px;">
      <div class="form-row" style="margin:0;">
        <label>Date</label>
        <input type="date" name="entryDate" value="<%= test && test.testDate ? (new Date(test.testDate)).toISOString().slice(0,10) : (new Date()).toISOString().slice(0,10) %>" readonly>
      </div>
      <div class="form-row" style="margin:0;">
        <label>Time Requested</label>
        <input type="time" name="timeRequested" value="<%= test && test.testDate ? (new Date(test.testDate)).toTimeString().slice(0,5) : '' %>" readonly>
      </div>
      <div class="form-row" style="margin:0;">
        <label>Time Released</label>
        <input type="time" name="timeReleased" value="<%= test && test.completedAt ? (new Date(test.completedAt)).toTimeString().slice(0,5) : '' %>">
      </div>
    </div>

    <div style="display:flex;gap:12px;align-items:center;">
      <div style="flex:1;">
        <label>Sodium (Na)</label>
        <input type="number" step="0.1" name="sodium" placeholder="mmol/L" value="<%= test.results && (test.results.sodium || test.results.na) ? (test.results.sodium || test.results.na) : '' %>">
      </div>
      <div style="flex:1;">
        <label>Potassium (K)</label>
        <input type="number" step="0.01" name="potassium" placeholder="mmol/L" value="<%= test.results && (test.results.potassium || test.results.k) ? (test.results.potassium || test.results.k) : '' %>">
      </div>
      <div style="flex:1;">
        <label>Chloride (Cl)</label>
        <input type="number" step="0.1" name="chloride" placeholder="mmol/L" value="<%= test.results && (test.results.chloride || test.results.cl) ? (test.results.chloride || test.results.cl) : '' %>">
      </div>
    </div>

    <div style="margin-top:8px;" class="form-row">
      <label>Optional: Reference Values (e.g. 135-145) - leave blank to use defaults</label>
      <div style="display:flex;gap:8px;">
        <input type="text" name="sodium_ref" placeholder="Na ref" style="flex:1" value="<%= test.results && test.results.sodium_ref ? test.results.sodium_ref : '' %>">
        <input type="text" name="potassium_ref" placeholder="K ref" style="flex:1" value="<%= test.results && test.results.potassium_ref ? test.results.potassium_ref : '' %>">
        <input type="text" name="chloride_ref" placeholder="Cl ref" style="flex:1" value="<%= test.results && test.results.chloride_ref ? test.results.chloride_ref : '' %>">
      </div>
    </div>

    <hr>
    <h4>Signatures / Performers</h4>
    <div class="form-row">
      <label>Medical Technologist (Name)</label>
      <input type="text" name="mtName" value="<%= test.results && test.results.performedByName ? test.results.performedByName : '' %>">
    </div>
    <div class="form-row">
      <label>Medical Technologist License</label>
      <input type="text" name="mtLicense" value="<%= test.results && test.results.performedByLicense ? test.results.performedByLicense : '' %>">
    </div>

    <div class="form-row">
      <label>Pathologist (Name)</label>
      <input type="text" name="pathName" value="<%= test.results && test.results.requestedByName ? test.results.requestedByName : '' %>">
    </div>
    <div class="form-row">
      <label>Pathologist License</label>
      <input type="text" name="pathLicense" value="<%= test.results && test.results.requestedByLicense ? test.results.requestedByLicense : '' %>">
    </div>

    <hr>
    <h4>Validation</h4>
    <div class="form-row">
      <label>2nd Medical Technologist (Validator Name)</label>
      <input type="text" name="validatedByName" value="<%= test.results && test.results.validatedByName ? test.results.validatedByName : '' %>">
    </div>
    <div class="form-row">
      <label>2nd Medical Technologist License</label>
      <input type="text" name="validatedByLicense" value="<%= test.results && test.results.validatedByLicense ? test.results.validatedByLicense : '' %>">
    </div>

    <div class="form-row mt-12">
      <a href="/tests/<%= test.id %>" class="button btn-secondary">Cancel</a>
      <button class="button btn-primary">Save Results</button>
    </div>
  </form>

</div>
