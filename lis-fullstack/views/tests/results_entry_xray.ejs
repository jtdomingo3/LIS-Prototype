<div class="card">
  <h3>Enter X-Ray Results - <%= test.testId %></h3>

  <form action="/tests/<%= test.id %>/results" method="POST">
    <input type="hidden" name="_method" value="POST">

    <div style="display:flex;gap:12px;align-items:center;margin-bottom:8px;flex-wrap:wrap">
      <div class="form-row" style="margin:0;">
        <label>Date</label>
        <input type="date" name="entryDate" value="<%= test && test.testDate ? (new Date(test.testDate)).toISOString().slice(0,10) : (new Date()).toISOString().slice(0,10) %>" readonly>
      </div>
      <div class="form-row" style="margin:0;">
        <label>Time Requested</label>
        <input type="time" name="timeRequested" value="<%= test && test.testDate ? (new Date(test.testDate)).toTimeString().slice(0,5) : '' %>">
      </div>
      <div class="form-row" style="margin:0;">
        <label>Time Released</label>
        <input type="time" name="timeReleased" value="<%= test && test.completedAt ? (new Date(test.completedAt)).toTimeString().slice(0,5) : '' %>">
      </div>
    </div>

    <div class="form-row">
      <label>Case Number</label>
      <input type="text" name="caseNumber" value="<%= test && test.caseNumber ? test.caseNumber : '' %>">
    </div>

    <div class="form-row">
      <label>Examination (short)</label>
      <input type="text" name="examination" value="<%= test && (test.examination || test.examinationName) ? (test.examination || test.examinationName) : '' %>">
    </div>

    <div style="margin-top:8px">
      <label>Examination / Findings (paragraphs)</label>
      <div style="margin-bottom:6px; display:flex; gap:8px; align-items:center;">
        <button type="button" class="button btn-small" onclick="wrapSelection('<b>','</b>')">B</button>
        <button type="button" class="button btn-small" onclick="wrapSelection('<i>','</i>')"><em>I</em></button>
        <button type="button" class="button btn-small" onclick="wrapSelection('<u>','</u>')">U</button>
        <button type="button" class="button btn-small" onclick="insertParagraph()">Insert Paragraph</button>
      </div>
      <textarea id="paragraphs" name="paragraphs" rows="8" style="width:100%; font-family:inherit;"> <%= test && test.results && (test.results.paragraphs || test.results.result) ? (test.results.paragraphs || test.results.result) : '<p>Enter findings here. This may be 2-3 paragraphs depending on the reading.</p>\n<p>Describe radiographic findings, impressions, and recommendations.</p>' %></textarea>
    </div>

    <hr>
    <h4>Signatures / Performers</h4>
    <div class="form-row">
      <label>X-Ray Technologist (Name)</label>
      <input type="text" name="mtName" value="<%= test.results && test.results.performedByName ? test.results.performedByName : '' %>">
    </div>
    <div class="form-row">
      <label>Technologist License</label>
      <input type="text" name="mtLicense" value="<%= test.results && test.results.performedByLicense ? test.results.performedByLicense : '' %>">
    </div>

    <div class="form-row">
      <label>Radiologist (Name)</label>
      <input type="text" name="pathName" value="<%= test.results && test.results.requestedByName ? test.results.requestedByName : '' %>">
    </div>
    <div class="form-row">
      <label>Radiologist License</label>
      <input type="text" name="pathLicense" value="<%= test.results && test.results.requestedByLicense ? test.results.requestedByLicense : '' %>">
    </div>

    <div class="form-row mt-12">
      <a href="/tests/<%= test.id %>" class="button btn-secondary">Cancel</a>
      <button class="button btn-primary">Save Results</button>
    </div>
  </form>

  <script>
    function wrapSelection(openTag, closeTag) {
      var ta = document.getElementById('paragraphs');
      var start = ta.selectionStart;
      var end = ta.selectionEnd;
      var selected = ta.value.substring(start, end);
      var newText = openTag + selected + closeTag;
      ta.value = ta.value.substring(0, start) + newText + ta.value.substring(end);
      ta.focus();
      ta.selectionStart = start + openTag.length;
      ta.selectionEnd = start + openTag.length + selected.length;
    }
    function insertParagraph(){
      var ta = document.getElementById('paragraphs');
      var pos = ta.selectionStart || ta.value.length;
      var p = '<p>New paragraph...</p>';
      ta.value = ta.value.substring(0,pos) + p + ta.value.substring(pos);
      ta.focus();
    }
  </script>
</div>
